export type PlateType = "civilian" | "diplomatic" | "military";

export type DiplomaticVariant = "ambassador" | "diplomat" | "staff";

export interface LookupEntry {
  name: string;
  note?: string;
}
